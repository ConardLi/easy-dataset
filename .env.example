# Database Configuration
# MySQL connection string
# Note: Replace the password and database name with your actual values
# Special characters in password must be URL-encoded (@ = %40, # = %23, etc.)
DATABASE_URL="mysql://root:your_password@localhost:3306/your_database_name?authPlugin=mysql_native_password"
